export default function AboutPage() {
  return (
    <>
      <h1>About</h1>
      <p>
        This app exists for one reason: to be measured twice and produce the same numbers both
        times. If it does not, the measurement is wrong — not the app.
      </p>
      <p className="meta">Routes: /, /about, /blog, /blog/[slug], /dashboard</p>
    </>
  )
}
